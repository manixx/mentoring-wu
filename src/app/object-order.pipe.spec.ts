import { ObjectOrderPipe } from './object-order.pipe';

describe('ObjectOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
